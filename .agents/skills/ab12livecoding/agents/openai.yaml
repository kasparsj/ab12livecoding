interface:
  display_name: "Ab12livecoding"
  short_description: "Repo-specific patterns and workflows for ab12livecoding"
  default_prompt: "Use the ab12livecoding repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true